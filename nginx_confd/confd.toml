[template]
src = "nginx.tmpl"
dest = "/etc/nginx/nginx.conf"
keys = [
"/lb/backends/csphere-csphere/ips",
]
owner = "root"
mode = "0644"
check_cmd = "/usr/sbin/nginx -t -c /etc/nginx/nginx.conf"
reload_cmd = "/usr/sbin/nginx -s reload"
